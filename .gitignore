# The data lives in the database on the host (docs/database-migration.md);
# analysis/fetch_data.jl fills data/ with the extracts. Only the two
# hand-maintained files are tracked.
data/*
!data/ttfx_annotations.json
!data/methodology_changes.json

# Julia (Manifest.toml is tracked: the image instantiates from it)
*.jl.cov
*.jl.mem

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~
.vscode/
.idea/
.cache/

# Local database (db/)
*.sqlite
*.sqlite-wal
*.sqlite-shm
